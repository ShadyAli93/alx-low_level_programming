#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - for loop
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char l;
for (l = 'a'; l <= 'z'; l++)
putchar(l);
putchar('\n');
return (0);
}
